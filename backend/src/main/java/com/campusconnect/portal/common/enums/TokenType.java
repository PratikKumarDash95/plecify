package com.campusconnect.portal.common.enums;

/** Type of a one-time security token (email verification, password reset, login OTP). */
public enum TokenType {
    EMAIL_VERIFICATION,
    PASSWORD_RESET,
    LOGIN_OTP
}
